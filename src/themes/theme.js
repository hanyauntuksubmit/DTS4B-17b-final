import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        type: 'dark',
        primary: {
          main: '#1975ff',
        },
        secondary: {
          main: '#12b5c7',
        },
      },
});

export default theme;
